import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#B88961",
      light: "#ffffff",
    },
    secondary: {
      main: "#888888",
      light: "#F4F4F4",
    },
  },
});

export default theme;
